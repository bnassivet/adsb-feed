# Use the official Python 3.10 image as the base image
FROM python:3.10-slim

# Set the working directory
WORKDIR /app

# Copy the Python script to the working directory
COPY pulsar-client-async.py .

# Install the required dependencies
RUN pip install pulsar-client

# Define the command to run the Python script
CMD ["python", "pulsar-client-async.py"]

#In this Dockerfile, we use the official Python 3.10 image as the base image and set the working directory to /app. The Python script pulsar-client-async.py is copied to the working directory, and the required dependency pulsar-client is installed using pip.
#Finally, the command to run the Python script is defined as python pulsar-client-async.py. To build the Docker image, you can use the following command:
#docker build -t forwarder .
#And to run the Docker container, you can use the following command:
#docker run forwarder

